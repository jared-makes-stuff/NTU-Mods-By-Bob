/**
 * Feature-level re-export for timetable generation filter types.
 *
 * Keeps feature imports stable while sharing a single type source of truth.
 */
export type { FilterPreference, GenerationFilters } from "@/shared/types/timetableGeneration";
